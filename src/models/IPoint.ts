export interface Point {
    title: string;
    time: string;

    indexFrom: number;
    indexTo: number;
}

export interface MapsPoint {
    title: string;
	position: [number, number]
}
